1) CHANGE:
	 bool shoot(Char enemy, MissileWeapn wep){...}

IN: actors/hero/Hero.java

INTO:

public boolean shoot( Char enemy, MissileWeapon wep, Boolean brkInvi) {

		//temporarily set the hero's weapon to the missile weapon being used
		KindOfWeapon equipped = belongings.weapon;
		belongings.weapon = wep;
		rangedAttack = true;
		boolean result = attack( enemy );
		if(brkInvi){
			Invisibility.dispel();
		}
		belongings.weapon = equipped;
		rangedAttack = false;

		return result;
	}

2) CHANGE:
	void onThrow (int cell){...}

IN:items/weapon/missiles/MissileWeapon.java

INTO:

protected void onThrow( int cell ) {
		Char enemy = Actor.findChar( cell );
		if (enemy == null || enemy == curUser) {
				parent = null;
				super.onThrow( cell );
		} else {
			if (!curUser.shoot( enemy, this, true )) {
				rangedMiss( cell );
			} else {
				
				rangedHit( enemy, cell );

			}
		}
	}
3) MAKE:
	SilentThrowingKnives.java
@Override
protected void onThrow( int cell ) {
		Char enemy = Actor.findChar( cell );
		if (enemy == null || enemy == curUser) {
				parent = null;
				super.onThrow( cell );
		} else {
			if (!curUser.shoot( enemy, this, false)) {
				rangedMiss( cell );
			} else {
				
				rangedHit( enemy, cell );

			}
		}
	}

@Override
	public boolean isUpgradable() {
		if(lvl <=3){
			return true;
		}else{
			return false;
		}
	
	}	

4) The process is quite similar, make TheWhisperer.java - a hand gun that does not break stealth (chage in BulletGun.java instead)

5) Make GunPowder.java - items in crafting

6) FIX: Oil Flask




	